#!/bin/sh
# postinst script for gmediarender
#
# see: dh_installdeb(1)

set -e

case "$1" in
    configure)
#        if ! getent passwd gmediarender >/dev/null; then
#          adduser --disabled-password  --quiet --system \
#            --home /var/run/gmediarender --no-create-home \
#            --gecos "GMediaRender daemon" gmediarender
#          adduser --quiet gmediarender audio
#        fi
#	chown gmediarender /var/log/gmediarender
#	chmod u+w /var/log/gmediarender
    ;;

    abort-upgrade|abort-remove|abort-deconfigure)
    ;;

    *)
        echo "postinst called with unknown argument \`$1'" >&2
        exit 1
    ;;
esac

# dh_installdeb will replace this with shell code automatically
# generated by other debhelper scripts.

#DEBHELPER#

exit 0
